<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Format CSI Spec Section" Height="520" Width="450" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Text Box Width (inches):" VerticalAlignment="Center" Width="170" FontWeight="Bold"/>
            <TextBox x:Name="WidthInput" Text="5.5" Width="60" VerticalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Max Characters per Line:" VerticalAlignment="Center" Width="170" FontWeight="Bold"/>
                <TextBox x:Name="CharInput" Text="85" Width="60" VerticalContentAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Calculation: Width (in) × Font CPI = Max Characters" Foreground="Gray" FontSize="11" Margin="0,0,0,2"/>
            <TextBlock Text="Example: 5.5 in. × 15.5 CPI (Arial Narrow 10pt) ≈ 85 chars" Foreground="Gray" FontSize="11" Margin="0,0,0,2"/>
            <TextBlock Text="Note: Look up the Characters Per Inch (CPI) for your specific font and size." Foreground="#005A9E" FontSize="11" FontStyle="Italic" Margin="0,2,0,0"/>
        </StackPanel>

        <TextBox Grid.Row="2" x:Name="SpecTextInput" 
                 AcceptsReturn="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" 
                 Margin="0,0,0,15"
                 FontFamily="Consolas"
                 Text="Paste your raw specification text here..."/>

        <Button Grid.Row="3" x:Name="FormatBtn" Content="Format &amp; Place" Height="35" 
                FontWeight="Bold" Background="#0078D7" Foreground="White" 
                Click="format_and_place"/>
    </Grid>
</Window>